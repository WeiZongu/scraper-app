# キーワード検索・抽出ツール

CSSセレクタの指定は不要です。**検索キーワード**でWebを検索し、見つかったページの中から
**抽出キーワード（＝表の列）がすべてそろっている（AND条件）1件のレコード**を行として
拾い集める、キーワードだけで使えるツールです。設定（検索キーワード・抽出キーワード）を
JSONで保存しておき、GUIから選んで実行するだけでデータ収集 → プレビュー（表形式）→
表形式のPowerPointスライド出力まで行えます。

JavaScriptで動的にコンテンツを描画するサイト（SPA等）にも対応するため、
収集エンジンには [Playwright](https://playwright.dev/python/) を使用しています。
Web検索には [Serper.dev](https://serper.dev/) の Search API を使用しています
（検索エンジンのページを直接スクレイピングする方式は、bot検知やHTML構造の変化で
不安定になりやすいため、正式なAPIを使う方式にしています。クレジットカード登録
不要で使い始められるため採用しています）。

## 事前準備: Serper.dev APIキーの取得（クレジットカード不要）

1. https://serper.dev/ にアクセスし、アカウントを作成してログイン
   （執筆時点ではクレジットカード登録不要で、買い切りの無料クレジット2,500回分が
   もらえます。※無料クレジットの内容は変更される場合があるため、登録時の画面で
   最新の内容を確認してください）
2. ダッシュボードでAPIキーを発行し、控えておく
3. このAPIキーを、後述の環境変数 `SERPER_API_KEY` に設定します

## セットアップ

```bash
cd scraper_app
python -m venv venv
source venv/bin/activate   # Windowsは venv\Scripts\activate

pip install -r requirements.txt
playwright install chromium   # 初回のみ。Chromiumブラウザ本体をダウンロードします
```

## iPhoneだけで使う（クラウドにデプロイ）

このアプリはPlaywright（ブラウザ自動操作）を使うため、iPhone単体では動作しません。
自分のPCも使わずに済むように、クラウド上に一度だけデプロイしておき、
以降はiPhoneのSafariだけで使う方法です。

- **完全無料・超過時は自動課金されず単に使えなくなる運用にしたい場合** →
  [`ORACLE_DEPLOY.md`](./ORACLE_DEPLOY.md)（Oracle Cloud 永久無料枠、詳細手順あり）
- **多少の月額費用でもよく、手軽さ優先の場合** → 下記のRender手順

### Renderでのデプロイ（参考・有料枠推奨）
無料枠もありますが、スリープや再起動でのデータ消失があり、超過時は自動課金される
アカウント形態です。手軽さ重視ならこちら、課金リスクを避けたい場合は上記Oracle手順を
使ってください。

1. このフォルダの中身をGitHubリポジトリにpush
2. Renderにログイン →「New +」→「Web Service」→ 先ほどのリポジトリを選択
3. **Environment** は「Docker」を選択（同梱の `Dockerfile` が自動で使われます）
4. **Instance Type** は無料枠（Free）でOK
5. 環境変数を設定します:
   - `SERPER_API_KEY`: 上記「事前準備」で取得したAPIキー（**必須**。無いと検索できません）
   - `APP_PASSWORD`: 好きな文字列（強く推奨）。これを設定しないと、URLを知っている
     誰でもアプリを操作できてしまいます。
6. 「Create Web Service」でデプロイ開始（初回ビルドはブラウザ同梱のため数分かかります）

デプロイが終わると `https://xxxxx.onrender.com` のようなURLが発行されます。
Safariでこれを開き、共有メニュー→「ホーム画面に追加」でアプリのように起動できます。

### 無料枠の制約と対策
- **一定時間アクセスがないとスリープします。** 次回アクセス時に数十秒〜1分ほど起動待ちが発生します。
- **再起動・再デプロイのたびに保存データが消えます（ディスクが一時的なため）。**
  「サイト一覧」タブの **「設定をバックアップ」** で保存済みサイト設定をJSONファイルとしてダウンロードし、
  iPhoneのFilesアプリ等に保管しておいてください。消えてしまった場合は **「設定を復元」** から
  そのJSONファイルを選ぶだけで元に戻せます。
- 出力したPowerPointスライドも同様に一時保存です。生成後はその場でダウンロードして保存してください。
- **無料プランはメモリ512MB程度が上限**です。ブラウザでの画像・フォント読み込みを
  ブロックする、画像処理前に縮小するなどメモリを抑える対策をしていますが、
  それでも一度に開くページ数が多い・抽出キーワードが多いなどで処理が重くなると
  メモリ超過で実行中に落ちることがあります。発生した場合はRenderの「Events」タブに
  "Out of Memory" 等の表示が出ます。抽出キーワードを絞る、検索範囲を狭める等で
  軽減できます。

### Render以外の選択肢
[Railway](https://railway.app/) や [Fly.io](https://fly.io/) も同様にDockerfileから無料枠デプロイが可能です。
手順はほぼ同じ（Dockerfileを検出させ、環境変数 `SERPER_API_KEY` と `APP_PASSWORD` を
設定するだけ）です。

## （参考）自宅PCから使う場合

自宅にPC/Macがあり、それを常時起動しておける場合は、クラウドを使わずに
Tailscaleでも同じことができます。

### 起動

```bash
export SERPER_API_KEY=取得したAPIキー   # Windowsは set SERPER_API_KEY=...
python main.py
```

自動的にブラウザが立ち上がり、`http://localhost:8550` でGUIが表示されます
（デスクトップウィンドウではなく、ブラウザで動くWebアプリになっています）。

### Tailscale経由でiPhoneから使う

1. **PC側**
   - [Tailscale](https://tailscale.com/download) をインストールし、Googleアカウント等でログイン
   - タスクトレイのTailscaleアイコン→「This device」で `100.x.x.x` 形式のTailscale IPを確認
   - Windows セキュリティ（Defender ファイアウォール）で `python.exe`（またはポート8550）の
     受信を許可
   - `python main.py` でアプリを起動したままにしておく

2. **iPhone側**
   - App Storeから「Tailscale」アプリをインストールし、PCと同じアカウントでログイン
   - Safariで `http://<PCのTailscale IP>:8550` にアクセス
   - 共有メニュー→「ホーム画面に追加」で便利に

## 使い方

1. **「検索設定一覧」タブ**の「＋ 新規設定」から新しい設定を作成、または保存済み設定をタップして編集。
2. **「検索設定」タブ**を入力:
   - **設定名**: 一覧に表示される名前（任意の名前でOK）
   - **検索キーワード**: Webを検索するためのキーワード（例: `東京 ラーメン 新宿`）
   - **抽出キーワード**: 表の列にしたい語句をカンマ区切りで複数指定
     （例: `営業時間, 定休日, 住所`）。**指定したキーワードすべてを含む(AND条件)**
     1件のレコード（ページ内の1つのカード・1行等のまとまり）だけを結果の行として
     拾います。1つでも欠けているキーワードがあれば、その候補は行になりません。
     各列の値は、そのキーワードを含む文・段落そのものです。CSSセレクタの知識は不要です。
   - **多言語対応（自動・設定不要）**: 検索キーワード・抽出キーワードは常に
     日本語・英語・中国語(繁体字)・中国語(簡体字)・韓国語・スペイン語に自動翻訳され、
     日本語以外のページも検索・抽出の対象になります。抽出結果はどの言語で一致しても
     常に元のキーワードで表示されるため、サマリーやプレビューが言語ごとにバラバラに
     なることはありません。翻訳は無料サービス（[MyMemory](https://mymemory.translated.net/)）
     を利用しており、カード登録・APIキー登録は不要ですが、無料・非公式に近い性質上、
     利用状況によっては翻訳が失敗することがあります。失敗した場合は自動的に元の
     キーワードのみで続行します（実行ログに成功/失敗が表示されます）。多言語で検索する
     分、検索回数・処理ページ数が単一言語のときより増えるため、Serper.devの無料クレジット
     消費や処理時間もその分増えます。
3. 「設定を保存」で検索設定をJSONとして保存（次回以降、「検索設定一覧」タブから呼び出し可能）。
4. **「実行・結果」タブ**で「実行」。検索結果に見つかったページ（最大50件）を片っ端から
   開いて抽出します。検索 → 各ページを開いて抽出、の様子がログと表形式のプレビュー
   （抽出キーワードが列、最大200行）に表示されます。
5. 「スライド出力(PowerPoint)」でPowerPointファイルを生成し、表示されたリンクから
   ダウンロードします。
   - 構成は 表紙スライド → 表スライド（抽出キーワードを列とする表、**1ページ5行**、
     6行以上の場合は複数スライドに分割）です。
   - 表の列は「抽出キーワード（複数列）＋出典」で、出典列にはページタイトル・
     URL（リンク付き）・動画リンク（あれば）・取得日時をまとめて表示します。
   - セル内のテキストが長い場合は、枠内で折り返して表示されます（はみ出しません）。

### 抽出のしくみ（精度について）

CSSセレクタで正確に位置指定する方式とは違い、「抽出キーワードを含む、ある程度まとまった
テキストのかたまり」をヒューリスティック（経験則）で見つける方式です。手軽に使える一方で、
サイトのレイアウトによっては余分な文章が混ざったり、欲しい情報が拾えないことがあります。
抽出キーワードをより具体的な語句にすると精度が上がりやすいです。

ページ内で「1件分の情報のまとまり」（親要素1階層分）を単位とし、その中に**指定した
抽出キーワードがすべて含まれている場合だけ**（AND条件）、1行のレコードとして拾います。
1つでも欠けていれば、その候補は結果に含まれません。各列（キーワード）の値は、その
キーワードを含む文・段落そのものです。同じ内容のレコードがサイト内の複数ページに
またがって重複しないよう、全列の内容が完全に一致するものは自動的に1件にまとめられます。

さらに、数値部分だけが微妙に異なる重複（例:「営業時間 11:00〜21:00」と
「営業時間 11:00〜22:00」のように、ページごとに表記がわずかにブレている場合）は、
数値以外の列がすべて一致する行同士をグルーピングした上で、該当する列の数値部分を
中央値に置き換えて1件にまとめます。数値以外の内容が異なる行（別の店舗の情報など）は
まとめず、それぞれ別の行として残ります。

画像・動画も、この「1件分の情報のまとまり」と同じ場所（またはそのすぐ近くの親要素）に
あるものだけを拾います（PowerPointの表スライドには埋め込まれませんが、出典列の
動画リンクから確認できます）。ページ全体から無関係な画像を集めることはありません。

## サンプル設定

`site_configs/sample.json` に、動作確認用のサンプル設定を同梱しています。
起動後、「検索設定一覧」タブの「サンプル(書籍情報を探す)」から実行してみてください。

## 注意事項

- 対象サイトの利用規約・`robots.txt` を確認し、許可された範囲でご利用ください。
- Web検索には Serper.dev の Search API を利用しています。1回の検索で開くページ数は
  最大50件です（暴走防止と、買い切りクレジットを使いすぎないための上限）。
  無料クレジットを使い切ると検索結果が0件（またはそこまでの結果）で終了します
  （実行ログに表示されます）。
- 抽出はヒューリスティック（キーワード一致）ベースのため、100%正確な抽出は保証されません。
  抽出キーワードを具体的にすることで精度が上がります。
- ブラウザは常にheadless（画面を表示しない）で実行されます。クラウド環境には画面(Xサーバー)が
  無く、headlessをオフにすると必ず失敗するためです。
- スライド出力時に画像をダウンロードして埋め込むため、画像を含む件数が多いほど
  出力に時間がかかります。
- クラウド無料枠にデプロイして公開する場合は、`APP_PASSWORD` 環境変数を必ず設定してください
  （未設定だとURLを知る誰でも操作できてしまいます）。

## ディレクトリ構成

```
scraper_app/
├── main.py                 # Flet GUI 本体（パスワード認証・バックアップ機能含む）
├── Dockerfile               # Render/Railway等へのデプロイ用
├── .dockerignore
├── requirements.txt
├── scraper/
│   ├── config.py            # 検索設定の読み書き
│   ├── engine.py             # 検索・キーワード抽出処理（Playwright）
│   └── exporter.py           # PowerPointスライド出力
├── site_configs/            # 検索設定(JSON)保存先
│   └── sample.json
└── assets/                  # Fletが配信する静的ファイル置き場
    ├── output/               # 出力されたPowerPointファイル（ダウンロードリンクの実体）
    └── downloads/            # 検索設定バックアップJSONの置き場
```
